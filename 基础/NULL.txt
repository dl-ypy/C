二进制全部为零的含义 —— 0000000000000 的含义（看如何解码，不同解码对应不同含义）
    1.数值零
    2.字符串结束标记符‘\0’
    3.空指针NULL
          NULL表示的是零，但不代表数字0，而表示的是内存单元的编号为0

    我们计算机规定了，以零为编号的存储单元不可读，不可写
    如：free(p);
        p = NULL;   好处是下面不能再写*p